#include <stdio.h>

int main(void)
{
    int a;
    int b;
    int c;

    float d;
    float e;
    float f;

    printf("a: %d\n", a);
    printf("b: %d\n", b);
    printf("c: %d\n", c);
    printf("d: %f\n", d);
    printf("e: %f\n", e);
    printf("f: %f\n", f);

    return 0;
}

// there is no pattern with the int's but the float's are
// initalized to 0.000000