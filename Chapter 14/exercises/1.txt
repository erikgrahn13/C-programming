a) #define CUBE(x) ((x) * (x) * (x)) only arguments that are numbers work
b) #define REMINDER (n) ((n) % 4) only arguments that are numbers work.
c) #define PRODUCT (x, y) (((x) * (y))< 100 ? 1: 0) only arguments that are numbers work