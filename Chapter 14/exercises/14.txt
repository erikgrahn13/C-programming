int main(void)
{
    int a[= 10], i, j, k, m; // Compilation error

    i = j

    i = 10 * j + 1
    i = (x, y) x - y(j,k); // Compilation error
    i = ((j)*(j))*((j)*(j))
    i = (((j)*(j)) * (j))
    i = jk // Compilation error
    puts("i" "j");

    i = SQR(j);
    i = (j);

    return 0;
}